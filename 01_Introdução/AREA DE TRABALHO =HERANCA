




class cliente(pessoa):
    def __init__(self, nome, cpf, email):
            super().__init__(nome, cpf)
            self._email

class Funcionario:
    def __init(self, nome, CPF, matricula):
         pessoa.__init__(self, nome, CPF):
         self._nome = nomeself._matricula = matricula
         def getnome(self):
         return [self._nome, self,_matricula]

         cliente = cliente('Leo', 123, 'leo@gmail.com')
         print(cliente.getnome())
         Ffuncionario = funcionario('juca', 123, '10ab')
         print(funcionario1.getNome

   
    
    def getNome(self):
        return dict(
             nome = self.nome,
             cpf = self._cpf,
             email = self._email
        ) 
          
        
cliente1 = cliente('Leo', 123, 'leo@gmail.com')
print(cliente1.getNome())

# criar classe funcionario com nome cpf matricula
        
class Funcionario:

    def __init__(self, nome, cpf, matricula):
            self._nome = nome
            self._cpf = cpf
            self._matricula = matricula  

    def getNome(self):
        return self.getNome
          
        
cliente1 = cliente('Leo', 123, 'leo@gmail.com')
print(cliente1.getNome())

funcionario



